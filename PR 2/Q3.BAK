#include<stdio.h>
#include<conio.h>

main()
{
	int first,last,num;

	clrscr();

		printf("Enter Any Number:");
		scanf("%d",&num);
	last = num % 10;

	while(num>=10)
	{

			num= num / 10;

	}
			first=num;
	 printf("The sum of the first and the last digit:%d",first+last);


	getch();
}